import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  color: #fff;
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.6);
  position: relative;
`;

const Status = () => <Wrapper></Wrapper>;

export default Status;
